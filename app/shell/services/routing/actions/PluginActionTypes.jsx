// @flow
import keyMirror from 'keymirror'

/** Includes the list of all the actions supported by the SettingsManager */
export default keyMirror({
  MOUNT_INSTALLED_PLUGINS: null,
  UNMOUNT_PLUGIN: null,
})
